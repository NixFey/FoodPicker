@using Humanizer
@model List<MealWeek>

@{
    ViewBag.Title = "Week List";
}

<h2>Weeks</h2>
@if (Model.Any())
{
    @if (User.IsInRole("Admin"))
    {
        <a asp-route="WeekCreate">Create week</a>
    }
    <h3>Active Weeks</h3>
    <table class="table table-striped my-3">
        <thead>
        <tr>
            <th>Delivery Date</th>
            <th>Order Deadline</th>
            <th>Week Status</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var week in Model.Where(x => x.MealWeekStatus == MealWeekStatus.Active).OrderBy(x => x.DeliveryDate))
        {
            <tr>
                <td>@week.DeliveryDate.ToShortDateString()</td>
                <td>@week.OrderDeadline.Humanize()</td>
                <td>@Html.DisplayFor(x => week.MealWeekStatus)</td>
                <td>
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-route="WeekEdit" asp-route-id="@week.Id">Edit</a>
                    }
                    <a asp-action="Vote" asp-route-id="@week.Id">Vote</a>
                    <a asp-action="ViewResults" asp-route-weekId="@week.Id">View Results</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
    
    <h3>Inactive Weeks</h3>
    <table class="table table-striped my-3">
        <thead>
        <tr>
            <th>Delivery Date</th>
            <th>Order Deadline</th>
            <th>Week Status</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var week in Model.Where(x => x.MealWeekStatus != MealWeekStatus.Active).OrderByDescending(x => x.DeliveryDate))
        {
            <tr>
                <td>@week.DeliveryDate.ToShortDateString()</td>
                <td>@week.OrderDeadline.Humanize()</td>
                <td>@Html.DisplayFor(x => week.MealWeekStatus)</td>
                <td>
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-route="WeekEdit" asp-route-id="@week.Id">Edit</a>
                    }                    <a asp-action="ViewResults" asp-route-weekId="@week.Id">View Results</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
else
{
    <text>No weeks found. <a asp-route="WeekCreate">Create one?</a></text>
}
    
